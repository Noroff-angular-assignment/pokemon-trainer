import { Component } from '@angular/core';
import { NgForm } from '@angular/forms';

@Component({
  selector: 'app-fetch-actions',
  templateUrl: './fetch-actions.component.html',
  styleUrls: ['./fetch-actions.component.css']
})
export class FetchActionsComponent {

  constructor(
    ) {   }

    public fetchSubmit(): void {

    }

}
